{
  "name": "@polyboard/worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@polyboard/analytics": "workspace:*",
    "@polyboard/db": "workspace:*",
    "@polyboard/polymarket": "workspace:*",
    "pino": "^9.9.0",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "pino-pretty": "^13.1.1"
  }
}
